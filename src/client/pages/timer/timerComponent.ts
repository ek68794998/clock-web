import { Component } from "@angular/core";

@Component({
    moduleId: `${module.id}`,
    templateUrl: "timerComponent.html",
    styleUrls: [ "timerComponent.scss" ],
})
export class TimerComponent {
    constructor() {
    }
}
